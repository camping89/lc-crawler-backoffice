var abp = abp || {};

abp.modals.crawlerProxyEdit = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("BackOffice");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
